# Vercel server-only settings. Never expose these with a VITE_ prefix.
APPS_SCRIPT_BRIDGE_URL=
APPS_SCRIPT_BRIDGE_SECRET=
TOKEN_SIGNING_SECRET=

# Local-only switch for the classroom-safe demo flow.
VITE_ENABLE_DEMO_MODE=true
